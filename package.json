{
  "name": "web-page-loading",
  "version": "1.0.31",
  "description": "模拟页面加载进度的插件，支持amd、cmd、commonjs！同时使用v-v-pageloading适配Vue等前端主流框架",
  "main": "index.js",
  "scripts": {
    "browser": "browser-sync start --server --files '**/*.*'",
    "dev": "webpack-dev-server --devtool eval --progress  --hot --content-base docs",
    "test": "mocha --reporter spec --timeout 2000 --recursive tests/"
  },
  "keywords": [
    "web-page-loading"
  ],
  "author": "delen",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^2.6.1"
  }
}
